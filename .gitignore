.next
node_modules
dist
coverage
.DS_Store
*.log
.env
.env.local
.env.*.local
playwright-report
test-results

# build / cache
out
.cache
.tmp
temp
tsconfig.tsbuildinfo

# local database / prisma
prisma/dev.db
prisma/dev.db-journal
*.sqlite
*.sqlite-journal

# vercel / deployment leftovers
.vercel

# deployment staging / artifacts
deploy-stage
*.zip

# pm2 logs
pids
*.pid
*.pid.lock

# OS / editor
Thumbs.db
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea

# local tunnel / misc
.localtunnel

# material de origem das fotos da home (fonte bruta, nao versionar)
fotos para home/

# catalogos de origem (PDFs pesados, fonte bruta, nao versionar)
# wildcard cobre o acento decomposto (NFD) do nome da pasta no macOS/iCloud
cat*logos para o site/